name: Version

on:
  workflow_dispatch:

jobs:
  version:
    runs-on: ubuntu-22.04
    name: Version
    steps:
      - name: Release new version
        run: npm version minor

      - name: Push tag
        run: git push --tags
